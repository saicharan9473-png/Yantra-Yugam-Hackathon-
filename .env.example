PORT=3000
# Local MongoDB, OR use memory for dev (no install): MONGODB_URI=memory
MONGODB_URI=mongodb://127.0.0.1:27017/iteragen
JWT_SECRET=change-this-to-a-long-random-string
# Optional: ByteDance TRAE / custom codegen HTTP endpoint
# POST body: { "prompt": string, "fields": array }
# Expected JSON: { "html": "...", "css": "...", "js": "..." } or { "scaffold": "<full html doc>" }
TRAE_API_URL=
TRAE_API_KEY=
